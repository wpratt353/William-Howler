import styled from 'styled-components'

export const NavWrap = styled.div`
  position: fixed;
  top: 0;
  left: 0;
  width: 4rem;
  height: 100vh;
  z-index: 998;
  background-color: rgba(0, 0, 0, 0.1);
`
